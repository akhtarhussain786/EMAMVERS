---
deployment:
  tasks:
    - export DEPLOYPATH=/home/yatharth/staging.yatharthinstitution.in/
    - /bin/cp -R * $DEPLOYPATH 2>/dev/null || true
    - /bin/cp -n $DEPLOYPATH.env.staging $DEPLOYPATH.env 2>/dev/null || true
    - /bin/chmod 755 $DEPLOYPATH admin api database storage 2>/dev/null || true
    - /bin/chmod 644 .htaccess index.html 2>/dev/null || true
